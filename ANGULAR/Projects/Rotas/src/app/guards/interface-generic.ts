//Observação: a implementação de interfaces não é possivel no JS apenas no TS


export interface trocarRota{
    //A implementação desse metodo sera feita aonde ele for usado na classe
    podeDesativar(): boolean;
}